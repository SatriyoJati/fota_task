1. create ota task esp idf with esp_https_ota and native ota with rollback and validation using esp idf v5.4 

2. is it hard if we want to create wifi provisioning and configuration portal combined using one webserver and html static file using esp idf 5.5.4

3. i mean using wifi_prov_mgr using esp32 as AP then serve http webserver and static html for interacting. But i want also that static hmtl has other functionality other than just wifi provisioning, it can also has button to perform OTA then it will invoke my OTA component


4. sorry i forgot to mention , the credentials stored in NVS with KEYS, and the ota task actually comes in component using esp idf style project